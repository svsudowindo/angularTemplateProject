export const PROPERTIES = {

    // Any base url of the application which is common for all request
    BASE_URL: 'https://jsonplaceholder.typicode.co/',

    // Counts to retry the remote service if fails
    RETRY_SERVICE_COUNT: 0
};

